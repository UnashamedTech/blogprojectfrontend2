'use server';

// import apiCall from "@/base-api/api";

// const BASE_URL = `` //define the base url here from .env

export const fetchUsers = async () => {
  // const endpoint = `${BASE_URL}/....` //endpoint of the specific user fetching url
  // const response = await apiCall({url:endpoint}) // this is the api calling if the method is Get no need to add as a paramenter but
  //if it is Post Patch or Delete u will add apicall({url:endpoint, Method:"DELETE"}) if there is addition of data
  // like POST with data go for apiCall({url:endpoint, Method:"Post", data: data}) the data will be inside the async({data:type})
  // then
  //return response
  // then call this and use it in the
};
