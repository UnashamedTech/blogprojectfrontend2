export interface LayoutProps {
    children: React.ReactNode;
}
export interface Admin {
    id: string;
    name: string;
    age: number;
    gender: string;
    email: string;
    phoneNumber: string;
    location: string;
    status: string;
    profileImage: string;
}
