export interface inviteUserProps {
  name: string;
  email: string;
}

export interface addCategoryProps {
  title: string;
  description: string;
}

export type updateCategoryProps = {
  title: string;
  description: string;
};
